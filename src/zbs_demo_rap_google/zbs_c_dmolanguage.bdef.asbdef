projection implementation in class ZBP_BS_C_DEMO_LANGUAGE unique;
strict ( 2 );
use draft;

define behavior for ZBS_C_DMOLanguage alias Language
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action TranslateText;
}